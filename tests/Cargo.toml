[package]
name = "tests"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
sketch = { path = "../sketch" }
wasm-bindgen = "0.2.74"

[profile.release]
lto = true
